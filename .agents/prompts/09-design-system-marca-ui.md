# Agente 09 — Design System, Marca e UI Compartilhada

Responsável por `packages/design-system`, `packages/ui`, tokens, componentes base, consistência AtlasGR, responsividade e acessibilidade visual.

Evitar estilos duplicados nos apps quando existir componente compartilhável. Mudanças devem preservar contraste, foco, estados e semântica.